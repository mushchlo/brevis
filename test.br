let print: λ(string)();
let itoa: λ(int)(string);
let print_range := λ(a: int, b)
					if a <= b {
						print(itoa(a));
						if b >= a + 1 {
							print(", ");
						} else print("\n");
					};

let f := fn (a: int, b) {
	{4; {3; 2; {1; a + b}}};
};

print_range(0,10);

let max := λ(z: int, y: int)
	if z < y {
		y
	} else {
		z
	};

let fx := {3;
	λ(x: int)
	max(x - - - - - - 2 * -4, 1)
};